# Plataforma de gestión de cocina — dependencias
sqlalchemy>=2.0          # base de datos (SQLite para empezar, PostgreSQL en producción)
fastapi>=0.110           # API y páginas web
uvicorn>=0.27            # servidor
jinja2>=3.1              # plantillas HTML
python-multipart>=0.0.9  # subida de fotos
openpyxl>=3.1            # hojas de registro en Excel para imprimir
pillow>=11.3             # las fotos de las etiquetas: orientación, tamaño y EXIF fuera
cryptography>=42         # cifrado de los datos de contacto (con GRILL_DATA_KEY)
# Las zonas horarias, en el propio programa y no en el sistema. Windows no
# trae ninguna, y las imágenes «slim» de Linux tampoco siempre: si falta, una
# casa en «Europe/Budapest» trabaja con la hora corrida, y en marzo y octubre
# eso es una hora entera de apuntes en el día que no es. Se instala en todas
# partes a propósito, sin marcador de plataforma: así la hora de la casa no
# depende de lo que traiga el sistema donde toque desplegar.
tzdata>=2024.1

# desarrollo
pytest>=8
httpx>=0.27              # cliente de pruebas
# Playwright —el navegador con el que se miden las pantallas— no está aquí a
# propósito. De este fichero sale `requirements.lock`, y el lock es lo que se
# instala en el servidor: meterlo aquí le añadiría cuarenta megas a la imagen
# para algo que no se usa en producción. Se instala aparte donde hace falta:
# en el servidor de integración lo hace `.github/workflows/pruebas.yml`, y en
# un ordenador basta con `pip install playwright && playwright install
# chromium`. Sin él, las pruebas de pantalla se saltan solas; en el servidor
# de integración, en cambio, se caen, que es lo que hay que hacer allí.

# fases posteriores (importadores e informes), aún no usadas por el código:
# pdfplumber             # leer PDFs del POS
# weasyprint             # informe diario en PDF
