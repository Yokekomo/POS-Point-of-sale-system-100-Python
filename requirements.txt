# Plataforma de gestión de cocina — dependencias
sqlalchemy>=2.0          # base de datos (SQLite para empezar, PostgreSQL en producción)
fastapi>=0.110           # API y páginas web
uvicorn>=0.27            # servidor
jinja2>=3.1              # plantillas HTML
python-multipart>=0.0.9  # subida de fotos
openpyxl>=3.1            # hojas de registro en Excel para imprimir
cryptography>=42         # cifrado de los datos de contacto (con GRILL_DATA_KEY)

# desarrollo
pytest>=8
httpx>=0.27              # cliente de pruebas

# fases posteriores (importadores e informes), aún no usadas por el código:
# pdfplumber             # leer PDFs del POS
# weasyprint             # informe diario en PDF
